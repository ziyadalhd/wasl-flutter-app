# Wasl - Backend (Spring Boot)

Backend API لتطبيق Wasl مبني باستخدام Spring Boot.

## 🚀 البدء السريع

### المتطلبات
- Java 17 أو أعلى
- Maven (مضمن مع المشروع)
- Database (حسب الإعدادات)

### التثبيت والتشغيل

1. **انتقل إلى مجلد Backend:**
   ```bash
   cd backend
   ```

2. **تشغيل التطبيق:**
   ```bash
   # باستخدام Maven Wrapper (موصى به)
   ./mvnw spring-boot:run
   
   # على Windows
   mvnw.cmd spring-boot:run
   ```

3. **الوصول إلى API:**
   - الـ API سيكون متاح على: `http://localhost:8080`
   - Swagger UI (إذا مفعّل): `http://localhost:8080/swagger-ui.html`

## 🏗️ هيكلة المشروع

```
backend/
├── src/
│   ├── main/
│   │   ├── java/            # الكود المصدري
│   │   └── resources/       # ملفات الإعدادات
│   └── test/                # الاختبارات
├── pom.xml                  # Maven dependencies
└── mvnw                     # Maven Wrapper
```

## 🔧 أوامر مفيدة

```bash
# تشغيل التطبيق
./mvnw spring-boot:run

# بناء JAR
./mvnw clean package

# تشغيل الاختبارات
./mvnw test

# تنظيف Build artifacts
./mvnw clean
```

## ⚙️ الإعدادات

راجع ملف `src/main/resources/application.properties` أو `application.yml` للإعدادات.

### متغيرات البيئة المهمة
- `DATABASE_URL`: رابط قاعدة البيانات
- `SERVER_PORT`: منفذ الخادم (افتراضي: 8080)
- راجع ملف `.env.example` إن وجد

## 📡 API Endpoints

راجع Swagger documentation أو ملفات Controller للـ endpoints المتاحة.

## 🗄️ قاعدة البيانات

تأكد من إعداد قاعدة البيانات حسب ملف الإعدادات قبل التشغيل.

## 🐛 استكشاف الأخطاء

### المشكلة: Port already in use
**الحل:** غيّر المنفذ في `application.properties`:
```properties
server.port=8081
```

### المشكلة: Database connection error
**الحل:** تأكد من:
- قاعدة البيانات شغالة
- بيانات الاتصال صحيحة في `application.properties`

## 📝 ملاحظات مهمة

⚠️ **تنبيه:** لازم تكون داخل مجلد `backend/` قبل تشغيل أي أمر Maven!

```bash
# ❌ خطأ - من الجذر
./mvnw spring-boot:run

# ✅ صحيح - من داخل backend
cd backend
./mvnw spring-boot:run
```

## 🤝 المساهمة

راجع الـ README الرئيسي في الجذر للمزيد من المعلومات.
